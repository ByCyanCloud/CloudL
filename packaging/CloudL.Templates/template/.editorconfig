# 代码风格约定（业务项目）
#
# 这份文件由 CloudL 模板生成，目的与框架仓库里的同名文件一致：
# 让"什么算风格问题"由仓库说了算，而不是取决于每台机器上 IDE 的设置
# （否则同一个文件在不同人机器上会报不同的告警，例如 IDE0040）。
#
# 严重级别刻意都用 suggestion / silent：业务代码不应该因为风格提示而构建失败。
# 想收紧的话，把下面的 severity 改成 warning 即可。

root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

[*.{json,yml,yaml,xml,csproj,props,targets,slnx}]
indent_size = 2

[*.md]
trim_trailing_whitespace = false

[*.cs]
# 接口成员不写 public（C# 默认）—— 这条同时让 IDE0040 不再对接口成员报错
dotnet_style_require_accessibility_modifiers = for_non_interface_members:suggestion

dotnet_style_qualification_for_field = false:suggestion
dotnet_style_qualification_for_property = false:suggestion
dotnet_style_qualification_for_method = false:suggestion
dotnet_style_qualification_for_event = false:suggestion

dotnet_sort_system_directives_first = true
dotnet_separate_import_directive_groups = false

csharp_style_namespace_declarations = file_scoped:suggestion

dotnet_diagnostic.IDE0055.severity = suggestion
csharp_style_var_for_built_in_types = false:suggestion
csharp_style_var_when_type_is_apparent = false:suggestion
csharp_style_var_elsewhere = false:suggestion
csharp_style_expression_bodied_methods = when_on_single_line:suggestion
csharp_style_expression_bodied_properties = when_on_single_line:suggestion
csharp_prefer_braces = when_multiline:suggestion
csharp_style_prefer_primary_constructors = false:suggestion
